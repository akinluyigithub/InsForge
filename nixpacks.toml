# Nixpacks configuration for InsForge
# Ensures the project is built as a Node.js monorepo and ignores Deno artifacts in the root.

[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
